data di questo momento

```python
datetime.now()
```

`type()` si puo usare per i tipi di dato semplici

```python
x = 3
type(x) == int # True
```

ma anche per le classi

```python
class Persona
    pass
p = Persona()
type(p) == Persona # True
```
